- name: "HIGH | V-1073 | AUDIT | Systems must be maintained at a supported service pack level."
  win_command: reg query HKLM\System\CurentControlSet\control\windows /v CSDVersion
  register: supported_pack_level_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in supported_pack_level_audit.stderr and supported_pack_level_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-1073

- name: "HIGH | V-1074 | AUDIT | An approved DoD antivirus program must be installed and used."
  win_command: reg query HKLM\Software\McAfee\DesktopProtection /v szProductVer
  register: mcafee_approved_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in mcafee_approved_audit.stderr and mcafee_approved_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-1074

- name: "HIGH | V-1081 | AUDIT | Local volumes must be formatted using NTFS."
  win_shell: Get-Volume | Select-Object -Property FileSystem
  register: local_volume_format_aduit
  check_mode: no
  changed_when: no
  tags:
      - cat1
      - high
      - audit
      - V-1081

- name: "HIGH | V-1093 | AUDIT | Anonymous enumeration of shares must be restricted."
  win_command: reg query HKLM\System\CurrentControlSet\Control\Lsa /v RestrictAnonymous
  register: anon_shares_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in anon_shares_audit.stderr and anon_shares_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-1093

- name: "HIGH | V-1093 | PATCH | Anonymous enumeration of shares must be restricted."
  win_regedit: 
    key: 'HKLM:\System\CurrentControlSet\Control\Lsa'
    value: RestrictAnonymous
    data: 1
    datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-109

- name: "HIGH | V-1152 | AUDIT | Anonymous access to the registry must be restricted."
  win_command: reg query HKLM\SYSTEM\CurrentControlSet\Control\SecurePipeServers\Winreg
  register: anon_registry_access_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in anon_registry_access_audit.stderr and anon_registry_access_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-1152

- name: "HIGH | V-1152 | PATCH | Anonymous access to the registry must be restricted."
  win_regedit:
    key: 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurePipeServers\Winreg\'
  tags:
       - cat1
       - high
       - patch
       - V-1152

- name: "HIGH | V-1152 | AUDIT | Anonymous access to the registry must be restricted."
  win_shell: Get-Acl -PATH 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurePipeServers\Winreg' | Format-List
  register: anon_registry_access_audit2
  check_mode: no
  changed_when: no
  tags:
      - cat1
      - high
      - audit
      - V-1152

- name: "HIGH | V-1152 | PATCH | Anonymous access to the registry must be restricted."
  win_acl:
    path: 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurePipeServers\Winreg\'
    user: 'BUILTIN\Administrators'
    rights: 'FullControl'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  tags:
       - cat1
       - high
       - patch
       - V-1152

- name: "HIGH | V-1152 | PATCH | Anonymous access to the registry must be restricted."
  win_acl:
    path: 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurePipeServers\Winreg\'
    user: 'BUILTIN\Backup Operators'
    rights: 'ReadKey'
    type: 'allow'
    state: 'present'
  changed_when: no
  tags:
       - cat1
       - high
       - patch
       - V-1152

- name: "HIGH | V-1152 | PATCH | Anonymous access to the registry must be restricted."
  win_acl:
    path: 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurePipeServers\Winreg\'
    user: 'NT AUTHORITY\LOCAL SERVICE'
    rights: 'ReadKey'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  tags:
       - cat1
       - high
       - patch
       - V-1152

- name: "HIGH | V-1153 | AUDIT | The LanMan authentication level must be set to send NTLMv2 response only, and to refuse LM and NTLM."
  win_command: reg query HKLM\System\CurrentControlSet\Control\Lsa /v LMCompatibilityLevel
  register: lanMan_authentication_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in lanMan_authentication_audit.stderr and lanMan_authentication_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-1153

# - name: "HIGH | V-1153 | PATCH | The LanMan authentication level must be set to send NTLMv2 response only, and to refuse LM and NTLM."
#   win_regedit:
#     key: 'HKLM:\System\CurrentControlSet\Control\Lsa'
#     value: LMCompatibilityLevel
#     data: 5
#     datatype: dword
#   tags:
#       - cat1
#       - high
#       - patch
#       - V-1153

- name: "HIGH | V-1159 | AUDIT | The Recovery Console option must be set to prevent automatic logon to the system."
  win_command: reg query \"HKLM\Software\Microsoft\Windows NT\CurrentVersion\Setup\RecoveryConsole\" /v SecurityLevel
  register: recovery_prevent_autologin_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in recovery_prevent_autologin_audit.stderr and recovery_prevent_autologin_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-1159

- name: "HIGH | V-1159 | PATCH | The Recovery Console option must be set to prevent automatic logon to the system."
  win_regedit:
    key: 'HKLM:\Software\Microsoft\Windows NT\CurrentVersion\Setup\RecoveryConsole\'
    value: SecurityLevel
    data: 0
    datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-1159

- name: "HIGH | V-2374 | AUDIT | Autoplay must be disabled for all drives."
  win_command: reg query HKLM\Software\Microsoft\Windows\CurrentVersion\policies\Explorer\ /v NoDriveTypeAutoRun 
  register: autoplay_disabled_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in autoplay_disabled_audit.stderr and autoplay_disabled_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-2374

- name: "HIGH | V-2374 | PATCH | Autoplay must be disabled for all drives."
  win_regedit:
      key: 'HKLM:\Software\Microsoft\Windows\CurrentVersion\policies\Explorer\'
      value: NoDriveTypeAutoRun
      data: 255
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-2374

- name: "HIGH | V-3338 | AUDIT | Named pipes that can be accessed anonymously must be configured with limited values on domain controllers."
  win_command: reg query HKLM\System\CurrentControlSet\Services\LanManServer\Parameters\ /v NullSessionPipes
  register: pipes_limited_dc_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in pipes_limited_dc_audit.stderr and pipes_limited_dc_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-3338

- name: "HIGH | V-3338 | PATCH | Named pipes that can be accessed anonymously must be configured with limited values on domain controllers."
  win_regedit:
    key: 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters\'
    value: NullSessionPipes
    data: \0netlogon\0samr\0lsarpc\0\0
    datatype: multistring
  tags:
      - cat1
      - high
      - patch
      - V-3338

- name: "HIGH | V-3339 | AUDIT | Unauthorized remotely accessible registry paths must not be configured."
  win_command: reg query \"HKLM\System\CurrentControlSet\Control\SecurePipeServers\ winreg\" /v Machine
  register: remotely_accessible_registry_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in remotely_accessible_registry_audit.stderr and remotely_accessible_registry_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-3339

#This may need to be implemented using something other than a registry edit
- name: "HIGH | V-3339 | PATCH | Unauthorized remotely accessible registry paths must not be configured."
  win_regedit:
    key: 'HKLM:\System\CurrentControlSet\Control\SecurePipeServers\ winreg'
    value: Machine
    data: \0System\CurrentControlSet\Control\ProductOptions\0\"System\CurrentControlSet\Control\Server Applications\"\0\"Software\Microsoft\Windows NT\CurrentVersion\"\0\0
    datatype: multistring
  changed_when: no
  tags:
      - cat1
      - high
      - patch
      - V-3339

- name: "HIGH | V-3340 | AUDIT | Network shares that can be accessed anonymously will not be allowed."
  win_command: reg query HKLM\System\CurrentControlSet\Services\LanManServer\Parameters /v NullSessionShares
  register: null_session_shares_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in null_session_shares_audit.stderr and null_session_shares_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-3340

- name: "HIGH | V-3340 | PATCH | Network shares that can be accessed anonymously will not be allowed."
  win_regedit:
      key: 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters'
      value: NullSessionShares
      data: ''
      datatype: multistring
  tags:
      - cat1
      - high
      - patch
      - V-3340

- name: "HIGH | V-3343 | AUDIT | Solicited Remote Assistance must not be allowed."
  win_command: reg query \"HKLM\Software\Policies\Microsoft\Windows NT\Terminal Services\" /v fAllowToGetHelp
  register: solicited_remote_assistance_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in solicited_remote_assistance_audit.stderr and solicited_remote_assistance_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-3343

- name: "HIGH | V-3343 | PATCH | Solicited Remote Assistance must not be allowed."
  win_regedit:
      key: 'HKLM:\Software\Policies\Microsoft\Windows NT\Terminal Services'
      value: fAllowToGetHelp
      data: 0
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-3343

- name: "HIGH | V-3344 | AUDIT | Local accounts with blank passwords must be restricted to prevent access from the network."
  win_command: reg query HKLM\System\CurrentControlSet\Control\Lsa\ /v LimitBlankPasswordUse
  register: blankpass_network_restriction_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in blankpass_network_restriction_audit.stderr and blankpass_network_restriction_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-3344

- name: "HIGH | V-3344 | PATCH | Local accounts with blank passwords must be restricted to prevent access from the network."
  win_regedit:
      key: 'HKLM:\System\CurrentControlSet\Control\Lsa\'
      value: LimitBlankPasswordUse
      data: 1
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-3344

- name: "HIGH | V-3379 | AUDIT | The system will be configured to prevent the storage of the LAN Manager hash of passwords."
  win_command: reg query HKLM\System\CurrentControlSet\Control\Lsa /v NoLMHash
  register: no_LM_has_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in no_LM_has_audit.stderr and no_LM_has_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-3379

- name: "HIGH | V-3379 | PATCH | The system will be configured to prevent the storage of the LAN Manager hash of passwords."
  win_regedit:
      key: 'HKLM:\System\CurrentControlSet\Control\Lsa'
      value: NoLMHash
      data: 1
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-3379

- name: "HIGH | V-4443 | AUDIT | Unauthorized remotely accessible registry paths and sub-paths must not be configured."
  win_command: reg query HKLM\System\CurrentControlSet\Control\SecurePipeServers\Winreg\AllowedPaths\ /v Machine
  register: unauthorized_remote_registry_paths_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in unauthorized_remote_registry_paths_audit.stderr and unauthorized_remote_registry_paths_audit.stderr" 
  tags:
      - cat1
      - high
      - audit
      - V-4443

- name: "HIGH | V-4443 | PATCH | Unauthorized remotely accessible registry paths and sub-paths must not be configured."
  win_regedit:
      key: 'HKLM:\System\CurrentControlSet\Control\SecurePipeServers\Winreg\AllowedPaths\'
      value: Machine
      data: \0\"Software\Microsoft\OLAP Server\"\0\"Software\Microsoft\Windows NT\CurrentVersion\Perflib\"\0\"Software\Microsoft\Windows NT\CurrentVersion\Print\"\0\"Software\Microsoft\Windows NT\CurrentVersion\Windows\"\0System\CurrentControlSet\Control\ContentIndex\0System\CurrentControlSet\Control\Print\Printers\0\"System\CurrentControlSet\Control\Terminal Server\"\0\"System\CurrentControlSet\Control\Terminal Server\UserConfig\"\0\"System\CurrentControlSet\Control\Terminal Server\DefaultUserConfiguration\"\0System\CurrentControlSet\Services\Eventlog\0System\CurrentControlSet\Services\Sysmonlog\0\0
      datatype: multistring
  tags:
      - cat1
      - high
      - patch
      - V-4443

- name: "HIGH | V-6834 | AUDIT | Anonymous access to Named Pipes and Shares must be restricted."
  win_command: reg query HKLM\System\CurrentControlSet\Services\LanManServer\Parameters\ /v RestrictNullSessAccess
  register: anon_access_pipes_shares_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in anon_access_pipes_shares_audit.stderr and anon_access_pipes_shares_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-6834

- name: "HIGH | V-6834 | PATCH | Anonymous access to Named Pipes and Shares must be restricted."
  win_regedit:
      key: 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters\'
      value: RestrictNullSessAccess
      data: 1
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-6834

- name: "HIGH | V-21973 | AUDIT | Autoplay must be turned off for non-volume devices."
  win_command: reg query HKLM\Software\Policies\Microsoft\Windows\Explorer\ /v NoAutoplayfornonVolume
  register: autoplay_nonvolume_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in autoplay_nonvolume_audit.stderr and autoplay_nonvolume_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-21973

- name: "HIGH | V-21973 | PATCH | Autoplay must be turned off for non-volume devices."
  win_regedit:
      key: 'HKLM:\Software\Policies\Microsoft\Windows\Explorer\'
      value: NoAutoplayfornonVolume
      data: 1
      datatype: dword
  tags:
      - cat1
      - high
      - patch

- name: "HIGH | V-22692 | AUDIT | The default Autorun behavior must be configured to prevent Autorun commands."
  win_command: reg query HKLM\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer\ /v NoAutorun
  register: autorun_prevent_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in autorun_prevent_audit.stderr and autorun_prevent_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-22692

- name: "HIGH | V-22692 | PATCH | The default Autorun behavior must be configured to prevent Autorun commands."
  win_regedit:
      key: 'HKLM:\Software\Microsoft\Windows\CurrentVersion\Policies\Explorer\'
      value: NoAutorun
      data: 1
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-22692

- name: "HIGH | V-26070 | AUDIT | Standard user accounts must only have Read permissions to the Winlogon registry key."
  win_shell: Get-Acl -PATH 'HKLM:\Software\Microsoft\Windows NT\CurrentVersion\Winlogon' | Format-List
  register: standard_user_read_audit
  check_mode: no
  changed_when: no
  tags:
      - cat1
      - high
      - audit
      - V-26070

- name: "HIGH | V-26070 | PATCH | Standard user accounts must only have Read permissions to the Winlogon registry key."
  win_acl:
    path: 'HKLM:\Software\Microsoft\Windows NT\CurrentVersion\Winlogon'
    user: "{{ item }}"
    rights: 'FullControl'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  with_items:
      - 'NT SERVICE\TrustedInstaller'
      - 'NT AUTHORITY\SYSTEM'
      - 'BUILTIN\Administrators'
  tags:
      - cat1
      - high
      - patch
      - V-26070

- name: "HIGH | V-26070 | PATCH | Standard user accounts must only have Read permissions to the Winlogon registry key."
  win_acl:
    path: 'HKLM:\Software\Microsoft\Windows NT\CurrentVersion\Winlogon'
    user: "{{ item }}"
    rights: 'ReadKey'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  with_items:
      - 'BUILTIN\Users'
      - 'ALL APPLICATION PACKAGES'
  tags:
      - cat1
      - high
      - patch
      - V-26070

- name: "HIGH | V-26283 | AUDIT | Anonymous enumeration of SAM accounts must not be allowed."
  win_command: reg query HKLM\System\CurrentControlSet\Control\Lsa\ /v RestrictAnonymousSAM
  register: anonymous_enum_sam_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in anonymous_enum_sam_audit.stderr and anonymous_enum_sam_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-26283

- name: "HIGH | V-26283 | PATCH | Anonymous enumeration of SAM accounts must not be allowed."
  win_regedit:
      key: 'HKLM:\System\CurrentControlSet\Control\Lsa\'
      value: RestrictAnonymousSAM
      data: 1
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-26283

- name: "HIGH | V-32282 | AUDIT | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_command: reg query {{ item }}
  register: standard_read_active_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in standard_read_active_audit.stderr and standard_read_active_audit.stderr"
  with_items:
      - \"HKLM\Software\Microsoft\Active Setup\Installed Components\"
      - \"HKLM\Software\Wow6432Node\Microsoft\Active Setup\Installed Components\"
  tags:
      - cat1
      - high
      - audit
      - V-32282

- name: "HIGH | V-32282 | PATCH | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_regedit:
    key: "{{ item }}"
  with_items:
      - 'HKLM:\Software\Microsoft\Active Setup\Installed Components\'
      - 'HKLM:\Software\Wow6432Node\Microsoft\Active Setup\Installed Components\'
  tags:
      - cat1
      - high
      - patch
      - V-32282

- name: "HIGH | V-32282 | AUDIT | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_shell: Get-Acl -PATH 'HKLM:\SOFTWARE\Microsoft\Active Setup\Installed Components' | Format-List
  register: standard_read_active_audit2
  check_mode: no
  changed_when: no
  tags:
      - cat1
      - high
      - audit
      - V-32282

- name: "HIGH | V-32282 | AUDIT | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_shell: Get-Acl -PATH 'HKLM:\SOFTWARE\Wow6432Node\Microsoft\Active Setup\Installed Components' | Format-List
  register: standard_read_active_audit3
  check_mode: no
  changed_when: no
  tags:
      - cat1
      - high
      - audit
      - V-32282

- name: "HIGH | V-32282 | PATCH | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_acl:
    path: "{{ item[0] }}"
    user: "{{ item[1] }}"
    rights: 'ReadKey'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  with_nested:
      - {'HKLM:\SOFTWARE\Microsoft\Active Setup\Installed Components\','HKLM:\SOFTWARE\Wow6432Node\Microsoft\Active Setup\Installed Components\'}
      - {'BUILTIN\Users', 'ALL APPLICATION PACKAGES'}
  tags:
      - cat1
      - high
      - patch
      - V-32282

- name: "HIGH | V-32282 | PATCH | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_acl:
    path: "{{ item[0] }}"
    user: "{{ item[1] }}"
    rights: 'FullControl'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  with_nested:
      - {'HKLM:\SOFTWARE\Microsoft\Active Setup\Installed Components\','HKLM:\SOFTWARE\Wow6432Node\Microsoft\Active Setup\Installed Components\'}
      - {'BUILTIN\Administrators', 'NT AUTHORITY\SYSTEM'}
  tags:
      - cat1
      - high
      - patch
      - V-32282

#ContainerInherit and InheritOnly set to satisfy (subkeys only)
- name: "HIGH | V-32282 | PATCH | Standard user accounts must only have Read permissions to the Active Setup Installed Components registry key."
  win_acl:
    path: "{{ item }}"
    user: 'CREATOR OWNER'
    rights: 'FullControl'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'InheritOnly'
  changed_when: no
  with_items:
      - 'HKLM:\SOFTWARE\Microsoft\Active Setup\Installed Components\'
      - 'HKLM:\SOFTWARE\Wow6432Node\Microsoft\Active Setup\Installed Components\'
  tags:
      - cat1
      - high
      - patch
      - V-32282

- name: "HIGH | V-34974 | AUDIT | The Windows Installer Always install with elevated privileges option must be disabled."
  win_command: reg query HKLM\Software\Policies\Microsoft\Windows\Installer\ /v AlwaysInstallElevated
  register: install_elevated_disabled_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in install_elevated_disabled_audit.stderr and install_elevated_disabled_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-34974

- name: "HIGH | V-34974 | PATCH | The Windows Installer Always install with elevated privileges option must be disabled."
  win_regedit:
      key: 'HKLM:\Software\Policies\Microsoft\Windows\Installer\'
      value: AlwaysInstallElevated
      data: 0
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-34974

- name: "HIGH | V-36718 | AUDIT | The Windows Remote Management (WinRM) service must not use Basic authentication."
  win_command: reg query HKLM\Software\Policies\Microsoft\Windows\WinRM\Service\ /v AllowBasic
  register: winrm_service_basic_auth_audit
  check_mode: no
  changed_when: no
  failed_when: "reg_not_found not in winrm_service_basic_auth_audit.stderr and winrm_service_basic_auth_audit.stderr"
  tags:
      - cat1
      - high
      - audit
      - V-36718

- name: "HIGH | V-36718 | PATCH | The Windows Remote Management (WinRM) service must not use Basic authentication."
  win_regedit:
      key: 'HKLM:\Software\Policies\Microsoft\Windows\WinRM\Service\'
      value: AllowBasic
      data: 0
      datatype: dword
  tags:
      - cat1
      - high
      - patch
      - V-36718

- name: "HIGH | V-39331 | AUDIT | The Active Directory SYSVOL directory must have the proper access control permissions."
  win_shell: Get-Acl -Path "\Windows\SYSVOL" | Format-List
  register: sysvol_permissions_audit
  check_mode: no
  changed_when: no
  tags:
      - cat1
      - high
      - audit
      - V-39331

- name: "HIGH | V-39331 | PATCH | The Active Directory SYSVOL directory must have the proper access control permissions."
  win_acl:
    path: '\Windows\SYSVOL'
    user: '{{ item }}'
    rights: 'ReadAndExecute'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  with_items:
      - NT AUTHORITY\Authenticated Users
      - BUILTIN\Server Operators
  tags:
      - cat1
      - high
      - patch
      - V-39331

- name: "HIGH | V-39331 | PATCH | The Active Directory SYSVOL directory must have the proper access control permissions."
  win_acl:
    path: '\Windows\SYSVOL'
    user: '{{ item }}'
    rights: 'FullControl'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'InheritOnly'
  changed_when: no
  with_items:
      - CREATOR OWNER
      - BUILTIN\Administrators
  tags:
      - cat1
      - high
      - patch
      - V-39331

- name: "HIGH | V-39331 | PATCH | The Active Directory SYSVOL directory must have the proper access control permissions."
  win_acl:
    path: '\Windows\SYSVOL'
    user: 'NT AUTHORITY\SYSTEM'
    rights: 'FullControl'
    type: 'allow'
    state: 'present'
    inherit: 'ContainerInherit, ObjectInherit'
    propagation: 'None'
  changed_when: no
  tags:
      - cat1
      - high
      - patch
      - V-39331
